"""Textual presenter package. Imports `cli.core` only; holds NO business
logic itself — `app.py` drives `core/`, `widgets.py` only renders data it
is given (design.md §2, ADR-001: headless core vs. thin TUI presenter).
"""
