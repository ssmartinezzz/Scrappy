"""Native CLI for fashion-scraper-new.

Headless core (`cli.core`) + presenters (`cli.tui`, `cli.plain`, added in
slice 2) that replace the duplicated `menu.ps1`/`menu.sh` REST clients.
See openspec/changes/native-cli-installer/design.md for the architecture.
"""
